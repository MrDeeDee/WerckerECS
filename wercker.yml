box: debian:latest
 
build:
  steps:
    - shellcheck:
        files: run.sh
publish:
  steps:
    - install-packages:
      packages: ca-certificates scp

    - internal/publish-step:
      owner: MrDeeDee