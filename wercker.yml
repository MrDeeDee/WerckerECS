box: debian:latest
 
build:
  steps:
    - shellcheck:
        files: run.sh
publish:
  steps:
    - install-packages:
      packages: ca-certificates git scp 

    - internal/publish-step:
      owner: MrDeeDee