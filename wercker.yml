box: debian:latest
 
build:
  steps:
    - shellcheck:
        files: run.sh
publish:
  steps:
    - install-packages:
      packages: scp ca-certificates 

    - internal/publish-step:
      owner: MrDeeDee